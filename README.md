# stack-using-c-dsa
This repository contains a C program that implements the Stack data structure using an array. The program demonstrates three main stack operations:  PUSH – Insert an element into the stack  POP – Remove an element from the stack  DISPLAY – Display all elements present in the stack  The stack follows the LIFO (Last In First Out) 
